apiVersion: v1
kind: Pod
metadata:
  name: hello-pod
spec:
  imagePullSecrets:
    - name: my-registry-secret
  containers:
  - name: hello-container
    image: hub.comcast.net/dx_svsops_platformoperations/alpine:latest
    imagePullPolicy: Always
    command: ["echo", "hello"]
